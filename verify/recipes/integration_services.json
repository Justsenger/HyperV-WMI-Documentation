{
  "id": "integration_services",
  "title": "切换集成服务组件启用状态",
  "status": "PASS",
  "code": "$ErrorActionPreference = 'Stop'\n$ns = 'root\\virtualization\\v2'\n$vmName = 'WMITEST_integration_services'\n\nfunction Wait-Job2($p){\n  if(-not $p){return 7}\n  $j=[wmi]$p\n  while($j.JobState -eq 3 -or $j.JobState -eq 4){ Start-Sleep -Milliseconds 200; $j=[wmi]$p }\n  return $j.JobState\n}\n\n$vsms = Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemManagementService\n\n# 创建第二代虚拟机\n$vssd = ([wmiclass]\"\\\\.\\$($ns):Msvm_VirtualSystemSettingData\").CreateInstance()\n$vssd.ElementName = $vmName\n$vssd.VirtualSystemSubType = 'Microsoft:Hyper-V:SubType:2'\n$r = $vsms.DefineSystem($vssd.GetText(1), $null, $null)\nif($r.ReturnValue -eq 4096){ Wait-Job2 $r.Job | Out-Null }\n$vm = [wmi]$r.ResultingSystem\n\n$vssd2 = $vm.GetRelated('Msvm_VirtualSystemSettingData') | Select-Object -First 1\n\n# 枚举集成组件设置数据 (每类均派生自 CIM_ResourceAllocationSettingData)\n$compClasses = @(\n  'Msvm_ShutdownComponentSettingData',\n  'Msvm_TimeSyncComponentSettingData',\n  'Msvm_HeartbeatComponentSettingData',\n  'Msvm_KvpExchangeComponentSettingData',\n  'Msvm_VssComponentSettingData',\n  'Msvm_GuestServiceInterfaceComponentSettingData'\n)\n$found = @{}\nforeach($c in $compClasses){\n  $inst = ([wmi]$vssd2.__PATH).GetRelated($c) | Select-Object -First 1\n  if($inst){ $found[$c] = $inst }\n}\n\n# 经 ModifyResourceSettings 切换某组件的 EnabledState (2=启用, 3=禁用)\n$comp = $found['Msvm_TimeSyncComponentSettingData']\n$before = [int]$comp.EnabledState\n$newState = if($before -eq 2){ 3 } else { 2 }\n$comp.EnabledState = [uint16]$newState\n$r2 = $vsms.ModifyResourceSettings($comp.GetText(1))\nif($r2.ReturnValue -eq 4096){ Wait-Job2 $r2.Job | Out-Null }\n\n# 重新 GetRelated 取新实例回读\n$vssd3 = $vm.GetRelated('Msvm_VirtualSystemSettingData') | Select-Object -First 1\n$comp2 = ([wmi]$vssd3.__PATH).GetRelated('Msvm_TimeSyncComponentSettingData') | Select-Object -First 1\nif([int]$comp2.EnabledState -eq $newState){ 'PASS' } else { 'FAIL' }\n\n# 清理\n$vsms.DestroySystem($vm.__PATH) | Out-Null",
  "output": "CREATED WMITEST_integration_services 7B61DC96-3BBD-455F-8027-B18D2CFD643A\n--- Integration components found on VM ---\nMsvm_ShutdownComponentSettingData EnabledState=2\nMsvm_TimeSyncComponentSettingData EnabledState=2\nMsvm_HeartbeatComponentSettingData EnabledState=2\nMsvm_KvpExchangeComponentSettingData EnabledState=2\nMsvm_VssComponentSettingData EnabledState=2\nMsvm_GuestServiceInterfaceComponentSettingData EnabledState=3\nMODIFY Msvm_TimeSyncComponentSettingData: 2 -> 3\nREADBACK Msvm_TimeSyncComponentSettingData EnabledState=3\nASSERT PASS: Msvm_TimeSyncComponentSettingData changed 2 -> 3\nRESULT=PASS\nCLEANUP destroyed WMITEST_integration_services rv=4096\nRESIDUAL NONE",
  "notes": [
    "集成组件以 Msvm_*ComponentSettingData 子类逐个通过 GetRelated 从虚拟机的 VSSD 获取，每类只有一个实例。",
    "全部 6 类均派生自 CIM_ResourceAllocationSettingData，只读属性 EnabledState：2=启用，3=禁用。修改后经 ModifyResourceSettings(GetText(1)) 下发，与修改普通资源同一路径。",
    "默认状态：Shutdown/TimeSync/Heartbeat/Kvp/Vss 默认为 2（启用），GuestServiceInterface 默认为 3（禁用）。",
    "ModifyResourceSettings 返回 4096 时表示异步作业，需经 Wait-Job2 轮询；回读须重新 GetRelated 取新实例，不能复用旧对象。",
    "新建第二代空虚拟机即带齐这些集成组件实例，无需先开机或安装来宾。",
    "ModifyResourceSettings 应传数组 @($comp.GetText(1)) 以匹配 ResourceSettings[] 签名；单参数依赖 WMI 强制转换亦可，但数组形式更规范。"
  ],
  "title_en": "Toggling the Enabled State of an Integration Services Component",
  "notes_en": [
    "The integration components are retrieved one by one from the VM's VSSD via GetRelated as Msvm_*ComponentSettingData subclasses, with a single instance per class.",
    "All 6 classes derive from CIM_ResourceAllocationSettingData, with the read-only property EnabledState: 2=Enabled, 3=Disabled. After modification, changes are submitted via ModifyResourceSettings(GetText(1)), following the same path as modifying an ordinary resource.",
    "Default states: Shutdown/TimeSync/Heartbeat/Kvp/Vss default to 2 (Enabled), while GuestServiceInterface defaults to 3 (Disabled).",
    "When ModifyResourceSettings returns 4096 it indicates an asynchronous job that must be polled with Wait-Job2; reading back requires obtaining a fresh instance via GetRelated again and must not reuse the old object.",
    "A newly created empty Generation 2 VM already comes with all of these integration component instances, without needing to be powered on first or having a guest installed.",
    "ModifyResourceSettings should be passed the array @($comp.GetText(1)) to match the ResourceSettings[] signature; passing a single argument and relying on WMI coercion also works, but the array form is more correct."
  ],
  "code_en": "$ErrorActionPreference = 'Stop'\n$ns = 'root\\virtualization\\v2'\n$vmName = 'WMITEST_integration_services'\n\nfunction Wait-Job2($p){\n  if(-not $p){return 7}\n  $j=[wmi]$p\n  while($j.JobState -eq 3 -or $j.JobState -eq 4){ Start-Sleep -Milliseconds 200; $j=[wmi]$p }\n  return $j.JobState\n}\n\n$vsms = Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemManagementService\n\n# Create a Generation 2 virtual machine\n$vssd = ([wmiclass]\"\\\\.\\$($ns):Msvm_VirtualSystemSettingData\").CreateInstance()\n$vssd.ElementName = $vmName\n$vssd.VirtualSystemSubType = 'Microsoft:Hyper-V:SubType:2'\n$r = $vsms.DefineSystem($vssd.GetText(1), $null, $null)\nif($r.ReturnValue -eq 4096){ Wait-Job2 $r.Job | Out-Null }\n$vm = [wmi]$r.ResultingSystem\n\n$vssd2 = $vm.GetRelated('Msvm_VirtualSystemSettingData') | Select-Object -First 1\n\n# Enumerate the integration component setting data (each class derives from CIM_ResourceAllocationSettingData)\n$compClasses = @(\n  'Msvm_ShutdownComponentSettingData',\n  'Msvm_TimeSyncComponentSettingData',\n  'Msvm_HeartbeatComponentSettingData',\n  'Msvm_KvpExchangeComponentSettingData',\n  'Msvm_VssComponentSettingData',\n  'Msvm_GuestServiceInterfaceComponentSettingData'\n)\n$found = @{}\nforeach($c in $compClasses){\n  $inst = ([wmi]$vssd2.__PATH).GetRelated($c) | Select-Object -First 1\n  if($inst){ $found[$c] = $inst }\n}\n\n# Toggle a component's EnabledState via ModifyResourceSettings (2=Enabled, 3=Disabled)\n$comp = $found['Msvm_TimeSyncComponentSettingData']\n$before = [int]$comp.EnabledState\n$newState = if($before -eq 2){ 3 } else { 2 }\n$comp.EnabledState = [uint16]$newState\n$r2 = $vsms.ModifyResourceSettings($comp.GetText(1))\nif($r2.ReturnValue -eq 4096){ Wait-Job2 $r2.Job | Out-Null }\n\n# GetRelated again to obtain a fresh instance and read back\n$vssd3 = $vm.GetRelated('Msvm_VirtualSystemSettingData') | Select-Object -First 1\n$comp2 = ([wmi]$vssd3.__PATH).GetRelated('Msvm_TimeSyncComponentSettingData') | Select-Object -First 1\nif([int]$comp2.EnabledState -eq $newState){ 'PASS' } else { 'FAIL' }\n\n# Cleanup\n$vsms.DestroySystem($vm.__PATH) | Out-Null"
}
