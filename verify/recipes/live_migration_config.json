{
  "id": "live_migration_config",
  "title": "读取并修改主机实时迁移服务设置",
  "status": "PASS",
  "code": "$ErrorActionPreference='Stop'\n$ns='root\\virtualization\\v2'\nfunction Wait-Job2($p){ if(-not $p){return 7}; $j=[wmi]$p; while($j.JobState -eq 3 -or $j.JobState -eq 4){Start-Sleep -Milliseconds 200; $j=[wmi]$p}; return $j.JobState }\n\n# --- 服务与单例设置数据 ---\n$svc=Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationService\n$s=Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationServiceSettingData\nif($s -is [array]){ $s=$s[0] }\n\n# --- 读取主机迁移配置 ---\n'EnableVirtualSystemMigration='+[bool]$s.EnableVirtualSystemMigration\n'MaximumActiveVirtualSystemMigration='+$s.MaximumActiveVirtualSystemMigration\n'MaximumActiveStorageMigration='+$s.MaximumActiveStorageMigration\n'AuthenticationType='+$s.AuthenticationType   # 0=CredSSP 1=Kerberos\n'EnableCompression='+[bool]$s.EnableCompression\n'EnableSmbTransport='+[bool]$s.EnableSmbTransport\n$svc.MigrationServiceListenerIPAddressList\nGet-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationNetworkSettingData  # 迁移网络\n\n# --- 无害修改: 翻转 EnableCompression 后再还原 ---\n$orig=[bool]$s.EnableCompression\n$s.EnableCompression = -not $orig\n$r=$svc.ModifyServiceSettings($s.GetText(1))   # IN=ServiceSettingData(内嵌 GetText(1)), OUT=Job; rv 0=成功 4096=作业\nif($r.ReturnValue -eq 4096){ Wait-Job2 $r.Job }\n\n# 还原(重新取新实例,写回原值)\n$s2=Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationServiceSettingData\nif($s2 -is [array]){ $s2=$s2[0] }\n$s2.EnableCompression=$orig\n$rr=$svc.ModifyServiceSettings($s2.GetText(1))\nif($rr.ReturnValue -eq 4096){ Wait-Job2 $rr.Job }",
  "output": "READ EnableVirtualSystemMigration=False\nREAD MaximumActiveVirtualSystemMigration=2\nREAD MaximumActiveStorageMigration=2\nREAD AuthenticationType=0\nREAD EnableCompression=True\nREAD EnableSmbTransport=False\nREAD MigrationNetworkCount=0\nREAD ListenerIPCount=1\nMODIFY(flip compression) ReturnValue=0\nREADBACK EnableCompression=False\nREVERT ReturnValue=0\nFINAL EnableCompression=True\nASSERT PASS: modified host migration setting and reverted successfully",
  "notes": [
    "纯主机配置操作,无需创建虚拟机; 本示例不涉及任何虚拟机。",
    "ModifyServiceSettings 的唯一 IN 参数为 ServiceSettingData(内嵌实例,须经 GetText(1) 序列化),OUT 为 Job。返回 ReturnValue=0 表示同步成功; 若返回 4096 则需 Wait-Job2 轮询。",
    "Msvm_VirtualSystemMigrationServiceSettingData 为主机级单例; 其属性不可直接写入,必须经 Msvm_VirtualSystemMigrationService.ModifyServiceSettings 下发。",
    "canonical 中标注为可写的属性为 EnableCompression 与 EnableSmbTransport; EnableVirtualSystemMigration、Maximum* 与 AuthenticationType 在类定义中标为只读,但文档说明其同样可经 ModifyServiceSettings 修改。",
    "修改设置时建议重新 Get 一份新实例再写回,避免使用脏实例。",
    "相关属性含义: EnableVirtualSystemMigration 控制迁移是否启用; AuthenticationType 0=CredSSP、1=Kerberos; MaximumActiveVirtualSystemMigration 与 MaximumActiveStorageMigration 为并发上限; MigrationServiceListenerIPAddressList 为监听地址列表; Msvm_VirtualSystemMigrationNetworkSettingData 为专用迁移网络配置。"
  ],
  "title_en": "Read and modify the host live migration service settings",
  "notes_en": [
    "A host-only configuration operation that requires no virtual machine; this example involves no virtual machine at all.",
    "The sole IN parameter of ModifyServiceSettings is ServiceSettingData (an embedded instance that must be serialized via GetText(1)), and the OUT parameter is Job. A ReturnValue of 0 indicates synchronous success; a return of 4096 requires Wait-Job2 polling.",
    "Msvm_VirtualSystemMigrationServiceSettingData is a host-level singleton; its properties cannot be written directly and must be applied through Msvm_VirtualSystemMigrationService.ModifyServiceSettings.",
    "The properties marked writable in canonical are EnableCompression and EnableSmbTransport; EnableVirtualSystemMigration, Maximum*, and AuthenticationType are marked read-only in the class definition, but the documentation states they can likewise be modified through ModifyServiceSettings.",
    "When modifying settings it is advisable to Get a fresh instance before writing back, to avoid using a dirty instance.",
    "Meaning of the relevant properties: EnableVirtualSystemMigration controls whether migration is enabled; AuthenticationType 0=CredSSP, 1=Kerberos; MaximumActiveVirtualSystemMigration and MaximumActiveStorageMigration are the concurrency limits; MigrationServiceListenerIPAddressList is the list of listener addresses; Msvm_VirtualSystemMigrationNetworkSettingData is the dedicated migration network configuration."
  ],
  "code_en": "$ErrorActionPreference='Stop'\n$ns='root\\virtualization\\v2'\nfunction Wait-Job2($p){ if(-not $p){return 7}; $j=[wmi]$p; while($j.JobState -eq 3 -or $j.JobState -eq 4){Start-Sleep -Milliseconds 200; $j=[wmi]$p}; return $j.JobState }\n\n# --- Service and singleton setting data ---\n$svc=Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationService\n$s=Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationServiceSettingData\nif($s -is [array]){ $s=$s[0] }\n\n# --- Read the host migration configuration ---\n'EnableVirtualSystemMigration='+[bool]$s.EnableVirtualSystemMigration\n'MaximumActiveVirtualSystemMigration='+$s.MaximumActiveVirtualSystemMigration\n'MaximumActiveStorageMigration='+$s.MaximumActiveStorageMigration\n'AuthenticationType='+$s.AuthenticationType   # 0=CredSSP 1=Kerberos\n'EnableCompression='+[bool]$s.EnableCompression\n'EnableSmbTransport='+[bool]$s.EnableSmbTransport\n$svc.MigrationServiceListenerIPAddressList\nGet-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationNetworkSettingData  # migration network\n\n# --- Harmless modification: flip EnableCompression, then revert ---\n$orig=[bool]$s.EnableCompression\n$s.EnableCompression = -not $orig\n$r=$svc.ModifyServiceSettings($s.GetText(1))   # IN=ServiceSettingData (embedded GetText(1)), OUT=Job; rv 0=success 4096=job\nif($r.ReturnValue -eq 4096){ Wait-Job2 $r.Job }\n\n# Revert (re-fetch a new instance, write back the original value)\n$s2=Get-WmiObject -Namespace $ns -Class Msvm_VirtualSystemMigrationServiceSettingData\nif($s2 -is [array]){ $s2=$s2[0] }\n$s2.EnableCompression=$orig\n$rr=$svc.ModifyServiceSettings($s2.GetText(1))\nif($rr.ReturnValue -eq 4096){ Wait-Job2 $rr.Job }"
}
